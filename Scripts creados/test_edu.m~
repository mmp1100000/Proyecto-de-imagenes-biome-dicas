%Test_Edu

I = select_image();

[X,Y] = ginput(2);
selectedImg = I;

c0=2;
initialLSF = c0*ones(size(I));
% generate the initial region R0 as two rectangles
initialLSF(X(1):X(2),Y(1):Y(2))=-c0;
phi=initialLSF;
contour(phi, [0,0], 'r','LineWidth',2);